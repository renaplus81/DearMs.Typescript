・interfaceとfunctionの違いがわからなかった
→interfaceはデータのかたを定義するもので、
functionは実際の処理(動的なアクション)を実行するもの。


・returnの位置がわかんない
・console.が出力してくれない
→インスタンスはクラスの外に出してください。

・インスタンス化が必要なのは、
クラス内にデータを保持したり、オブジェクトごとに異なる処理を行わせる必要がある場合。

🐻最初はcharacterというのを登場人物を作成するものかと思ってたけど、厳密に言えば登場人物の持っているもの(特性)を決めるものだった。


仮引数→呼び出し元からデータを受け取る関数の「受け皿」(google)
外部から受け取るデータの窓口（変数）として使われる(google)
これがあれば、決まった処理の型を
一つ作るだけで⭐️毎回異なる値(実引数)を柔軟に処理できる⭐️(google)
target: Characterは仮引数です。

・(target: Character)がなんなのか？
attack(target)という受け皿があり、
メソッドを呼ぶ側が「何かのcharacter」を渡し、
「target＝渡されたcharacter」になる。

console.log(yusha.attack(slime));だけだとでない。→voidがついてるから？

インスタンス化した名前やhpなどをattackで実行して欲しかったから入れようと思ったけど、
クラスの外で作ったslimeの変数をattackのメソッドの中で使うのは基本的に考えない
相手がスライムだけとは限らないので、ということでした。

thisの使い方がよくわからない。死んだ‼️

if文の中には
・勇者のダメージ数よりも敵のhpが多いならダメージを与えた文と残りhp表示
・targethpが0になったらxxを倒した文表示
・もし倒されたらgameover表示
がある。

returnの使い方わからない。
returnがエラーになる
returnがないとコンソールに表示されないのではなく、
returnがないと「呼び出し元に値を渡せない」
returnはその関数（メソッド）を呼びたした場所です。返す場所指定できない

🤖🤖chatgptの言う、「画面に表示する」処理と
「値を返す」処理は別物です。と言うのが意味がわからない。
→→→→attackはvoidなので返すものがありません？？空っぽのめも（return）
を渡している状態→undefinedが出た。わっつ？頭追いつかない世ー


returnでエラーになるのはvoidがあるからなんだけど、なんのためについてんの？？
chapyは「attackは値を返すメソッドではなく、攻撃処理をするメソッドとして作っている」
→これではわからないですよおおお

待って、定期的に直しながら実行しなかったせいでなんかreturn
エラーだけど１回実行してみようと思ってしたらなんか表示されたんだけどこういう感じで↓
勇者がスライムに50ダメージを与えた！(残り30HP。)
100←この100はなんですかという感想だけどなんで表示されたの？
return消してみる→Maximum call stack size exceeded
(コールスタックの最大のサイズ超え←は？)とりあえすreturnはねつけないと
ばかになってきた、わかんないよ＾ー