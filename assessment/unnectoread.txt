・interfaceとfunctionの違いがわからなかった
→interfaceはデータのかたを定義するもので、
functionは実際の処理(動的なアクション)を実行するもの。


・returnの位置がわかんない
・console.が出力してくれない
→インスタンスはクラスの外に出してください。

・インスタンス化が必要なのは、
クラス内にデータを保持したり、オブジェクトごとに異なる処理を行わせる必要がある場合。

🐻最初はcharacterというのを登場人物を作成するものかと思ってたけど、厳密に言えば登場人物の持っているもの(特性)を決めるものだった。


仮引数→呼び出し元からデータを受け取る関数の「受け皿」(google)
外部から受け取るデータの窓口（変数）として使われる(google)
これがあれば、決まった処理の型を
一つ作るだけで⭐️毎回異なる値(実引数)を柔軟に処理できる⭐️(google)
target: Characterは仮引数です。

・(target: Character)がなんなのか？
attack(target)という受け皿があり、
メソッドを呼ぶ側が「何かのcharacter」を渡し、
「target＝渡されたcharacter」になる。

console.log(yusha.attack(slime));だけだとでない。→voidがついてるから？